import React from "react";
import { Alert } from "react-bootstrap";

const Error = () => {
  return (
    <Alert variant="danger">
      Errore - Stai utilizzando il tuo Bearer token?
    </Alert>
  );
};

export default Error;
